
version: 2

sources:
      - name: sensor_data
        description: 'A database dbt model'
        database: sensor_data
        tables:
          - name: stations
            description: "A table dbt model"

models:
    - name: station_data
      description: "A starter dbt model"

    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
